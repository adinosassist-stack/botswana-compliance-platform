# Legacy BF-07 evidence — historical only

These files were recovered from the deleted pre-recovery repository and are retained only as historical evidence.
They refer to the former repository `adinosassist-stack/botswana-compliance-platform` and an old exact SHA/run.
They are **not valid release evidence for the V81 recovery successor** and must never satisfy a production seal gate.

The root-level `audit-report.json`, `bf07-evidence.json`, and `bf07-provenance.json` are intentionally absent until the
new GitHub BF-07 workflow resolves dependencies from a clean live npm registry state and generates fresh evidence for
the new exact recovery SHA.
