import assert from "node:assert/strict";import fs from "node:fs";const s=fs.readFileSync("cloudflare/migrations/052_v122_agent_observation_checkpoints.sql","utf8");assert.match(s,/agent_observation_checkpoints/);assert.match(s,/persistent_task_tenant_mismatch/);assert.match(s,/tenant_id,persistent_task_id,observed_at DESC/);console.log("v122 observation checkpoint schema passed");