import assert from "node:assert/strict";import {detectSnapshotChange,snapshotHash} from "../cloudflare/src/finance-watch-change-detector.js";const h=await snapshotHash({a:1});assert.equal((await detectSnapshotChange({previousHash:h,snapshot:{a:1}})).changed,false);assert.equal((await detectSnapshotChange({previousHash:h,snapshot:{a:2}})).changed,true);console.log("v126 finance watch change detector passed");