import fs from 'node:fs';
const worker=fs.readFileSync(new URL('../cloudflare/src/worker.js',import.meta.url),'utf8');
const html=fs.readFileSync(new URL('../public/index.html',import.meta.url),'utf8');
const migration=fs.readFileSync(new URL('../cloudflare/migrations/033_v78_preventive_action_effectiveness_verification.sql',import.meta.url),'utf8');
const schema=fs.readFileSync(new URL('../cloudflare/schema.sql',import.meta.url),'utf8');
const profile=JSON.parse(fs.readFileSync(new URL('../RELEASE_PROFILE.json',import.meta.url),'utf8'));
const pkg=JSON.parse(fs.readFileSync(new URL('../package.json',import.meta.url),'utf8'));
const sw=fs.readFileSync(new URL('../public/sw.js',import.meta.url),'utf8');
const checks=[];const check=(n,v)=>checks.push([n,!!v]);const patch=Number(pkg.version.split('.')[2]||0);
const helper=worker.slice(worker.indexOf('// v78 1.21.42 — control relapse prevention.'),worker.indexOf('async function executiveExceptionBrief(env,a){'));
const routes=worker.slice(worker.indexOf('if(url.pathname.match(/^\\/api\\/executive-control-preventive-actions'),worker.indexOf('if(url.pathname==="/api/executive-interventions"&&req.method==="POST"'));
const ui=html.slice(html.indexOf('<style id="v78-12143-preventive-action-effectiveness">'),html.indexOf('<style id="v78-12134-rereview-management">'))+html.slice(html.indexOf('function openControlPreventiveEffectiveness('),html.indexOf('function findAccountabilityItem'));
check('version 1.21.43+',pkg.version.startsWith('1.21.')&&patch>=43);check('profile aligned',profile.package_version===pkg.version);check('service worker aligned',sw.includes(pkg.version));
check('v1.21.43 gate preserved after successor',pkg.scripts['test:release-regressions'].includes('node tests/v78-12143-preventive-action-effectiveness-adversarial.mjs')&&pkg.scripts['test:release-regressions'].indexOf('v78-12144-preventive-action-pattern-learning-adversarial.mjs')<pkg.scripts['test:release-regressions'].indexOf('v78-12143-preventive-action-effectiveness-adversarial.mjs'));check('v1.21.43 runtime follows static gate',pkg.scripts['test:release-regressions'].indexOf('v78-12143-preventive-action-effectiveness-runtime.mjs')>pkg.scripts['test:release-regressions'].indexOf('v78-12143-preventive-action-effectiveness-adversarial.mjs'));check('dedicated script',pkg.scripts['test:v78-12143'].includes('v78-12143-preventive-action-effectiveness-adversarial.mjs')&&pkg.scripts['test:v78-12143'].includes('v78-12143-preventive-action-effectiveness-runtime.mjs'));
for(const k of ['preventive_action_effectiveness_verification','preventive_action_effectiveness_server_backed','preventive_action_effectiveness_evidence_required','preventive_action_effectiveness_attestation_required','preventive_action_effectiveness_drift_fails','preventive_action_effectiveness_warning_clears_only_after_pass','preventive_action_effectiveness_historical_bootstrap','preventive_action_effectiveness_leadership_only','preventive_action_effectiveness_embedded_existing_corrective_area','preventive_action_effectiveness_no_employee_scoring','preventive_action_effectiveness_no_manager_ranking','preventive_action_effectiveness_no_second_dashboard','preventive_action_effectiveness_no_second_notification_stream','preventive_action_effectiveness_no_routine_external_notifications','preventive_action_effectiveness_fail_closed'])check(`profile ${k}`,profile[k]===true);check('observation profile 7',profile.preventive_action_effectiveness_observation_days===7);
for(const x of ['CREATE TABLE IF NOT EXISTS executive_control_preventive_effectiveness_reviews','preventive_action_id TEXT NOT NULL','sustainability_review_id TEXT NOT NULL','corrective_action_id TEXT NOT NULL',"CHECK(status IN ('monitoring','ready','passed','failed','superseded'))",'observation_days INTEGER NOT NULL DEFAULT 7','observation_started_at TEXT NOT NULL','observation_due_at TEXT NOT NULL','baseline_counts_json TEXT NOT NULL','observed_counts_json TEXT','verification_evidence TEXT','verification_note TEXT','failure_reason TEXT','verified_at TEXT','verified_by_user_id TEXT','UNIQUE(tenant_id,preventive_action_id)','executive_control_preventive_effectiveness_status_idx','executive_control_preventive_effectiveness_action_idx'])check(`migration ${x}`,migration.includes(x));
check('observation bounded',migration.includes('CHECK(observation_days BETWEEN 3 AND 30)'));check('verified actor deletion safe',migration.includes('FOREIGN KEY(verified_by_user_id) REFERENCES users(id) ON DELETE SET NULL'));check('preventive cascade',migration.includes('FOREIGN KEY(preventive_action_id) REFERENCES executive_control_preventive_actions(id) ON DELETE CASCADE'));check('sustainability cascade',migration.includes('FOREIGN KEY(sustainability_review_id) REFERENCES executive_corrective_sustainability_reviews(id) ON DELETE CASCADE'));
for(const x of ['executive_control_preventive_effectiveness_reviews','executive_control_preventive_effectiveness_status_idx','executive_control_preventive_effectiveness_action_idx'])check(`schema ${x}`,schema.includes(x));
for(const x of ['function executiveControlPreventiveEffectivenessView(row)','async function executiveControlPreventiveEffectivenessRows(env,tenantId)','async function ensureExecutiveControlPreventiveEffectiveness(env,tenantId,preventiveRow,sustainRow,currentCounts)','async function refreshExecutiveControlPreventiveEffectiveness(env,tenantId,row,currentCounts)','observationDays:Number(row.observation_days||7)','effective:status==="passed"','observationComplete','daysRemaining'])check(`helper ${x}`,helper.includes(x));
check('completion-only eligibility',helper.includes('String(preventiveRow.status)!=="completed"'));check('unique lookup',helper.includes('tenant_id=? AND preventive_action_id=?'));check('7 day due',helper.includes('+7*86400000'));check('historical completion start',helper.includes('preventiveRow.completed_at||new Date().toISOString()'));check('historical bootstrap audit',helper.includes('historicalBootstrap:!!preventiveRow.completed_at'));check('warning retained for bootstrap',helper.includes("warning_started_at=COALESCE(warning_started_at,?)"));check('warning pending message',helper.includes('effectiveness observation is still required'));
check('effect drift uses baseline',helper.includes('executiveCorrectiveCountsAdvanced(observed,baseline)'));check('effect drift fails',helper.includes("status='failed'"));check('effect failure reason',helper.includes('New control drift appeared after preventive completion during the effectiveness observation period.'));check('effect fail audit',helper.includes('EXECUTIVE_CONTROL_PREVENTIVE_EFFECTIVENESS_FAILED'));check('ready only after due',helper.includes('dueMs<=Date.now()'));check('ready transition',helper.includes("status='ready'"));
check('history loaded before current prevention',helper.includes('preventiveHistory=preventBySustain'));check('completed preventive refreshed before new action',helper.indexOf('completedPreventive=preventiveHistory.find')<helper.indexOf('if(sustainabilityWarning&&!sustainabilityRelapsed)'));check('failed review can lead next prevention',helper.includes("if(String(pe.status)==='failed'&&!sustainabilityRelapsed)"));
for(const x of ['preventiveEffectivenessVerification:true','preventiveEffectivenessObservationDays:7','preventiveEffectivenessEvidenceRequired:true','preventiveEffectivenessAttestationRequired:true','preventiveWarningClearsOnlyAfterEffectivenessPass:true','preventiveEffectivenessDriftFails:true','preventiveEffectMonitoring','preventiveEffectReady','preventiveEffectPassed','preventiveEffectFailed'])check(`policy/count ${x}`,helper.includes(x));
check('completion route leadership only',routes.includes('if(!roleAllowed(a,"owner","manager"))return json({error:"forbidden"},403)'));check('completion evidence required',routes.includes('preventive_completion_evidence_required'));check('completion note required',routes.includes('preventive_completion_note_required'));check('completion attestation required',routes.includes('preventive_completion_attestation_required'));check('completion ownership guard',routes.includes('preventive_action_owned_by_another_manager'));check('completion rechecks full relapse',routes.includes('signal.fullRelapse'));check('completion creates effect review',routes.includes('ensureExecutiveControlPreventiveEffectiveness'));check('completion warning stays',routes.includes('warningCleared:false'));check('completion says monitoring',routes.includes('preventiveEffectivenessMonitoringStarted:true'));check('completion no baseline reset',routes.includes('doesNotResetEffectivenessBaseline:true'));check('completion no legal close',routes.includes('doesNotCloseUnderlyingLegalOrWorkflowAction:true'));check('completion no external',routes.includes('noExternalNotificationCreated:true'));
check('verification route exists',routes.includes('/effectiveness$/'));check('verification leadership only',routes.match(/effectiveness\$\/\)&&req.method==="POST"[\s\S]*?roleAllowed\(a,"owner","manager"\)/));check('verification evidence required',routes.includes('preventive_effectiveness_evidence_required'));check('verification note required',routes.includes('preventive_effectiveness_note_required'));check('verification attestation required',routes.includes('preventive_effectiveness_attestation_required'));check('verification requires completed action',routes.includes('preventive_action_not_completed'));check('verification rejects relapse',routes.includes('control_already_relapsed'));check('verification ownership guard',routes.includes('preventive_action_owned_by_another_manager'));check('verification refreshes live effect',routes.includes('refreshExecutiveControlPreventiveEffectiveness'));check('verification blocks failed',routes.includes('preventive_effectiveness_failed_drift'));check('verification blocks early',routes.includes('preventive_effectiveness_observation_incomplete'));check('verification requires ready',routes.includes('preventive_effectiveness_not_ready'));check('verification stores evidence',routes.includes('verification_evidence=?'));check('verification stores note',routes.includes('verification_note=?'));check('verification stores actor',routes.includes('verified_by_user_id=?'));check('verification stores timestamp',routes.includes('verified_at=CURRENT_TIMESTAMP'));check('verification clears warning only here',routes.includes('warning_started_at=NULL'));check('verification updates warning snapshot',routes.includes('warning_counts_json=?'));check('verification audit',routes.includes('EXECUTIVE_CONTROL_PREVENTIVE_EFFECTIVENESS_PASSED'));check('verification no external',routes.includes('noExternalNotificationCreated:true'));
check('GET attaches prevention effectiveness',routes.includes('preventEffectById'));check('GET exposes attached effectiveness',routes.includes('pv.effectiveness=preventEffectById.get'));check('GET policy verification',routes.includes('preventiveEffectivenessVerification:true'));check('GET policy warning pass only',routes.includes('preventiveWarningClearsOnlyAfterEffectivenessPass:true'));
for(const x of ['v78-12143-preventive-action-effectiveness','control-prevention-effectiveness','Verify preventive effectiveness','A completed preventive task is not proof of recovery','Warning clears only after a passed verification','controlPreventiveEffectivenessEvidence','controlPreventiveEffectivenessNote','controlPreventiveEffectivenessAttest','function openControlPreventiveEffectiveness(','function submitControlPreventiveEffectiveness()','preventive_effectiveness_observation_incomplete','preventive_effectiveness_failed_drift'])check(`ui ${x}`,html.includes(x));
check('UI completion observes recovery',html.includes('Preventive action completed · observing recovery'));check('UI completion not proof',html.includes('Completion alone does not clear the early-warning state'));check('UI monitoring button disabled',html.includes('Recovery observation in progress'));check('UI ready verify action',html.includes('Verify preventive effectiveness'));check('UI preview completion starts monitoring',html.includes('preview-preventive-effectiveness')&&html.includes('observationDays:7'));check('UI preview warning retained',html.includes('item.correctiveActionSustainabilityWarning=true'));check('UI preview verification clears warning',html.includes('x.item.correctiveActionSustainabilityWarning=false'));check('UI verification evidence validation',html.includes('Record objective verification evidence'));check('UI verification attestation validation',html.includes('Confirm the preventive-effectiveness attestation'));
check('embedded existing area',html.indexOf('control-prevention-effectiveness')>html.indexOf('executive-accountability'));check('mobile effectiveness action',html.includes('@media(max-width:620px){.control-prevention-effectiveness-actions .btn{width:100%}'));check('fail closed mentions preventive effectiveness',html.includes('preventive-effectiveness check unavailable')&&html.includes('preventive-effectiveness checks'));
for(const bad of ['manager leaderboard','employee scorecard','disciplinary recommendation'])check(`no ${bad}`,!ui.toLowerCase().includes(bad));
const failed=checks.filter(([,v])=>!v);for(const [n,v] of checks)console.log(`${v?'PASS':'FAIL'} ${n}`);console.log(`\n${checks.length-failed.length}/${checks.length} passed`);if(failed.length)process.exit(1);
