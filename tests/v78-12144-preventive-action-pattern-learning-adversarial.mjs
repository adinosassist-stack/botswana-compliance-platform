import fs from 'node:fs';const pkg=JSON.parse(fs.readFileSync(new URL('../package.json',import.meta.url))),profile=JSON.parse(fs.readFileSync(new URL('../RELEASE_PROFILE.json',import.meta.url))),sw=fs.readFileSync(new URL('../public/sw.js',import.meta.url),'utf8'),worker=fs.readFileSync(new URL('../cloudflare/src/worker.js',import.meta.url),'utf8'),schema=fs.readFileSync(new URL('../cloudflare/schema.sql',import.meta.url),'utf8'),migration=fs.readFileSync(new URL('../cloudflare/migrations/034_v78_preventive_action_pattern_learning.sql',import.meta.url),'utf8'),html=fs.readFileSync(new URL('../public/index.html',import.meta.url),'utf8');
const checks=[];const check=(n,v)=>checks.push([n,!!v]);const patch=Number(pkg.version.split('.')[2]||0);check('version 1.21.44+',pkg.version.startsWith('1.21.')&&patch>=44);check('profile aligned',profile.package_version===pkg.version);check('service worker aligned',sw.includes(pkg.version));check('v1.21.44 gate preserved after successor',pkg.scripts['test:release-regressions'].includes('node tests/v78-12144-preventive-action-pattern-learning-adversarial.mjs && node tests/v78-12144-preventive-action-pattern-learning-runtime.mjs')&&pkg.scripts['test:release-regressions'].indexOf('v78-12145-control-replacement-governance-adversarial.mjs')<pkg.scripts['test:release-regressions'].indexOf('v78-12144-preventive-action-pattern-learning-adversarial.mjs'));check('dedicated script',pkg.scripts['test:v78-12144'].includes('v78-12144-preventive-action-pattern-learning-adversarial.mjs')&&pkg.scripts['test:v78-12144'].includes('v78-12144-preventive-action-pattern-learning-runtime.mjs'));
for(const k of ['preventive_action_pattern_learning','weak_preventive_control_replacement_required','preventive_pattern_no_people_scoring'])check(`profile ${k}`,profile[k]===true);check('profile lookback 180',profile.preventive_pattern_lookback_days===180);
for(const x of ['CREATE TABLE IF NOT EXISTS executive_control_preventive_patterns','intervention_id TEXT NOT NULL',"CHECK(status IN ('replacement_required','resolved'))",'lookback_days INTEGER NOT NULL DEFAULT 180','preventive_action_count INTEGER NOT NULL DEFAULT 0','failed_effectiveness_count INTEGER NOT NULL DEFAULT 0','superseded_action_count INTEGER NOT NULL DEFAULT 0','repeated_trigger_kind TEXT','repeated_trigger_count INTEGER NOT NULL DEFAULT 0',"CHECK(severity IN ('high','critical'))",'reason TEXT NOT NULL','stats_json TEXT NOT NULL','detected_at TEXT NOT NULL','last_seen_at TEXT NOT NULL','resolved_by_user_id TEXT','UNIQUE(tenant_id,intervention_id)','executive_control_preventive_pattern_status_idx','executive_control_preventive_pattern_intervention_idx'])check(`migration ${x}`,migration.includes(x));check('deletion safe resolver',migration.includes('FOREIGN KEY(resolved_by_user_id) REFERENCES users(id) ON DELETE SET NULL'));check('tenant cascade',migration.includes('FOREIGN KEY(tenant_id) REFERENCES tenants(id) ON DELETE CASCADE'));check('intervention cascade',migration.includes('FOREIGN KEY(intervention_id) REFERENCES executive_exception_interventions(id) ON DELETE CASCADE'));check('schema includes table',schema.includes('executive_control_preventive_patterns'));check('schema includes indexes',schema.includes('executive_control_preventive_pattern_status_idx')&&schema.includes('executive_control_preventive_pattern_intervention_idx'));
for(const x of ['function executiveControlPreventivePatternStats(actionRows,effectByPreventive,sinceAt=null)','function executiveControlPreventivePatternView(row)','async function executiveControlPreventivePatternRows(env,tenantId)','async function ensureExecutiveControlPreventivePattern(env,tenantId,interventionId,stats)'])check(`helper ${x}`,worker.includes(x));check('lookback 180',worker.includes('Date.now()-180*86400000'));check('three action threshold',worker.includes('actionCount>=3'));check('two failed threshold',worker.includes('failedEffectiveness>=2'));check('two superseded threshold',worker.includes('supersededActions>=2'));check('repeat trigger threshold',worker.includes('repeatedTriggerCount>=3'));check('critical threshold',worker.includes("severity=(failedEffectiveness>=2||supersededActions>=2)?'critical':'high'"));check('failure reason included',worker.includes('preventive-effectiveness failures'));check('superseded reason included',worker.includes('preventive actions superseded by full relapse'));check('repeat trigger reason included',worker.includes('repeat ${String(repeatedTriggerKind'));check('unique lookup',worker.includes("tenant_id=? AND intervention_id=? LIMIT 1"));check('durable replacement required',worker.includes("status='replacement_required'"));check('resolved baseline supported',worker.includes("String(preventivePatternRow?.status)==='resolved'?preventivePatternRow?.resolved_at:null"));check('resolved baseline cutoff',worker.includes('Math.max(Date.now()-180*86400000'));check('resolved pattern reopens with audit',worker.includes('EXECUTIVE_CONTROL_PREVENTIVE_PATTERN_REOPENED'));check('weak pattern allows replacement corrective cycle',worker.includes('weakPreventivePattern=await env.DB.prepare'));check('old duplicate guard bypass only for weak pattern',worker.includes('&&!weakPreventivePattern)return json({error:"corrective_action_already_resolved_for_current_pattern"'));check('replacement opening audited',worker.includes('preventivePatternReplacement:!!weakPreventivePattern'));check('effectiveness query carries intervention',worker.includes('c.intervention_id,c.opened_at corrective_opened_at'));check('replacement effectiveness resolves pattern',worker.includes('EXECUTIVE_CONTROL_PREVENTIVE_PATTERN_REPLACEMENT_VERIFIED'));check('resolution requires replacement opened after detection',worker.includes('openedMs>=detectedMs'));check('resolution preserves history',worker.includes('historyPreserved:true'));check('new learning baseline after resolution',worker.includes('newLearningBaselineStartsAtResolution:true'));check('effectiveness response reports pattern resolution',worker.includes('weakPreventivePatternResolved'));check('system audit',worker.includes('EXECUTIVE_CONTROL_PREVENTIVE_PATTERN_DETECTED'));check('audit control level',worker.includes('controlDesignSignalOnly:true'));check('audit no employee score',worker.includes('noEmployeeScore:true'));check('audit no ranking',worker.includes('noManagerRanking:true'));check('audit no discipline',worker.includes('noDisciplinaryInference:true'));check('audit no external',worker.includes('noExternalNotificationCreated:true'));
check('history grouped across corrective cycles',worker.includes('correctiveToIntervention')&&worker.includes('preventByIntervention'));check('pattern uses all intervention prevention',worker.includes('executiveControlPreventivePatternStats(preventByIntervention.get(String(item.interventionId))'));check('pattern attached',worker.includes('item.correctiveActionPreventivePattern=preventivePattern'));check('replacement attached',worker.includes('item.correctiveActionPreventiveReplacementRequired=preventiveReplacementRequired'));check('weak pattern reopens root cause requirement',worker.includes('sustainabilityRelapsed||preventiveReplacementRequired'));check('stabilization blocked by weak pattern',worker.includes('&&!preventiveReplacementRequired'));check('weak pattern count',worker.includes('weakPreventivePatterns'));check('critical pattern count',worker.includes('criticalPreventivePatterns'));
for(const x of ['preventivePatternLearning:true','preventivePatternLookbackDays:180','weakPreventiveControlReplacementRequired:true','preventivePatternControlLevelOnly:true','preventivePatternNoPeopleScoring:true'])check(`policy ${x}`,worker.includes(x));
check('read endpoint',worker.includes('/api/executive-control-preventive-patterns'));check('endpoint owner manager only',worker.includes('if(!roleAllowed(a,"owner","manager"))return json({error:"forbidden"},403)'));check('endpoint refreshes brief',worker.includes('const brief=await executiveExceptionBrief(env,a)'));check('endpoint replacement count',worker.includes('replacementRequired:(rows.results||[])'));for(const x of ['controlDesignSignalOnly:true','replaceWeakControlInsteadOfRepeatingTemporaryFix:true','noEmployeeScoring:true','noManagerRanking:true','noDisciplinaryInference:true','noSecondDashboard:true','noRoutineExternalNotification:true'])check(`endpoint policy ${x}`,worker.includes(x));
for(const x of ['v78-12144-preventive-action-pattern-learning','preventive-control-pattern','Weak preventive-control pattern','replace or materially strengthen the control','control-design signal, not a manager or employee score','Repeating the same temporary preventive step does not resolve the pattern','preventive-pattern-tags','preventive-pattern-actions','function preventiveControlPatternSummary(x)'])check(`ui ${x}`,html.includes(x));check('ui successor governed corrective workflow',html.includes('Plan control replacement')&&html.includes('Open replacement corrective action')&&html.includes("openSystemicCorrectiveAction('${safeId(x.interventionId)}')"));check('ui existing area',html.indexOf('v78-12144-preventive-action-pattern-learning')>html.indexOf('executive-accountability'));check('ui mobile',html.includes('@media(max-width:620px){.preventive-pattern-actions .btn{width:100%}'));check('ui reason escaped',html.includes('escapeHtml(p.reason'));check('ui 180d tag',html.includes('preventive actions / ${Number(p.lookbackDays||180)}d'));check('ui failed tag',html.includes('failed verifications'));check('ui superseded tag',html.includes('superseded by relapse'));check('ui repeated tag',html.includes('repeat ${trigger}'));
for(const bad of ['manager leaderboard','employee scorecard','disciplinary recommendation'])check(`no ${bad} in new UI`,!html.slice(html.indexOf('<style id="v78-12144'),html.indexOf('<style id="v78-12143')).toLowerCase().includes(bad));
// Preserve predecessor controls and non-notification posture.
for(const x of ['preventiveEffectivenessVerification:true','preventiveEffectivenessObservationDays:7','preventiveWarningClearsOnlyAfterEffectivenessPass:true','preventiveEffectivenessDriftFails:true','repeatedDriftFullRelapse:true','sustainabilityWatchDays:90','noSecondNotificationStream:true','noRoutineExternalSustainabilityNotifications:true','noRoutineExternalPreventionNotifications:true'])check(`predecessor ${x}`,worker.includes(x));
const failed=checks.filter(([,v])=>!v);for(const [n,v] of checks)console.log(`${v?'PASS':'FAIL'} ${n}`);console.log(`\n${checks.length-failed.length}/${checks.length} passed`);if(failed.length)process.exit(1);
